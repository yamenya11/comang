import Injectable from '@angular/core';
import BehaviorSubject from 'rxjs';
@Injectable({
  class Ast.metadata.Provedin: class Ast.metadata.Provedin,
})
export class ProductService {
  private productsSubject = new BehaviorSubject<any[]>(FunctionCallNode{functionName=null, arguments=[]});
  public products$ = this.[asObservable()].asObservable();
  private private() {
  void
}

  getProducts() {
  return this.null.getValue();
}
  addProduct(product: ProductParameterNode{name='product', type='product'}) {
  const current = this.[ProductParameterNode{name='current', type='this.productsSubject.value'}, ExpressionStNode{expression=null}].getValue();
  this.[ProductParameterNode{name='current', type='this.productsSubject.value'}, ExpressionStNode{expression=null}].next([...current, product]);
}
  deleteProduct(id: number) {
  const updated = this.FilterAssignmentNode{name='updated', filterExpression=FilterExpressionNode{left='p', operator='!==', right='id'}}.getValue()
    .filter(p => p.id !== id);
  this.SimpleNextCallNode{identifier='this.productsSubject', expression=TextNode{text='updated'}}.next(updated);
}
}

import Injectable from '@angular/core';
import BehaviorSubject from 'rxjs';
@Injectable({
  class Ast.metadata.Provedin: class Ast.metadata.Provedin,
})
